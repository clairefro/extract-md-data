interface MdExtractConfig {
  slug?: {
    /** TODO: slugify config opts */
  };
}

export const mdExtract = (config: MdExtractConfig = {}) => {
  // get files
  // for each file
  // // extract json
  // // build relPath
  // // build slug
  // // make an id that is hash of relPath + slug
  // build relPaths, slugs,
};
